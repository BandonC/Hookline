// Endpoint CRUD route handlers. Mounted from src/index.ts.
// Secret is generated here (crypto.randomUUID or random bytes), stored plaintext,
// returned once on creation. GET/list must never select signing_secret.

// TODO: export Hono route handlers for create / list / delete.
export {};
